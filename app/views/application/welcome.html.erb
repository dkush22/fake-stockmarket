<% if !flash.empty? %>
<% flash.each do |key, value| %>
  <%= content_tag :div, value, class: "flash #{key}" %>
  <%end%>
  <% end %>
<h1>Welcome to YaKush.ly</h1>
<!-- login stuff coming later -->
<div><%= link_to "Sign Up", new_user_path%></div>
<div><%= link_to "Sign In", signin_path%></div>
